# 개선기획안 — 배포 후 개선 사이클 기록

1차 배포: 2026-07-27 · https://callcenter-ax.pages.dev
원칙: 완벽보다 배포 우선. 배포된 것 위에서 사이클을 돌며 개선한다.

## 사이클 0 — 1차 배포 시점의 상태 (기준선)

- 배포 검증 완료: 홈/화면 200, Whisper STT 라이브(합성 음성 → 2.7초 전사, 정답 35자 대비 편집거리 1자 = CER 2.9% (2026-07-28 재측정)),
  bge-m3 벡터 검색 라이브, 분석 데모 폴백의 강성 민원 감지 정상.
- 테스트 46개 통과 (QA 점수·CER·검색 랭킹·응답 계약·레이트리밋·Turnstile).
- LLM 3종(분석·QA 정성·RAG 답변)은 CLAUDE_API_KEY 미등록으로 데모 폴백 동작 중.

## 우선순위 백로그

### P1 — 심사 전 꼭 (마감 7/28)
- [ ] CLAUDE_API_KEY 시크릿 등록 → LLM 3종 라이브 전환 확인
  (`npx wrangler pages secret put CLAUDE_API_KEY --project-name callcenter-ax`)
- [ ] 사용자 본인 목소리 녹음 1건 이상으로 STT→분석→QA 전체 흐름 실기기 확인
  (대본: docs/녹음스크립트.md)
- [ ] 모바일 화면 점검 (심사자가 폰으로 열 확률 높음)

### P2 — 완성도
- [ ] Turnstile 위젯에 callcenter-ax.pages.dev 도메인 추가 + TURNSTILE_SECRET 등록
- [x] ~~/analyze 결과를 VOC 대시보드에 실시간 누적~~ → 사이클 2에서 localStorage로 구현 (D1 승격은 추후)
- [x] ~~/stt 브라우저 마이크 직접 녹음 (MediaRecorder)~~ → 사이클 2에서 구현
- [x] ~~Whisper 모델 2종 CER 비교~~ → 사이클 2에서 구현
- [ ] OG 이미지 제작 (링크 공유 시 미리보기)

### P3 — 확장
- [x] ~~Vectorize 도입: FAQ 문서 수십~수백 건 규모의 사전 인덱싱~~ → 사이클 26에서 구현
- [ ] 화자 분리(diarization) 또는 채널 분리 업로드 (상담사/고객 트랙)
- [x] ~~QA 체크리스트 편집 UI (콜센터별 커스텀 규칙)~~ → 사이클 14에서 구현
- [x] ~~텔레메트리 대시보드 (/about에 엔드포인트별 호출·지연 통계)~~ → 사이클 12에서 구현

## 사이클 기록

### 2026-07-31 · 사이클 39 (분석에서 운영 행동까지 — 키 없이 동작하는 결정적 층)

API 키 등록 전이라 **키 없이도 실제 값이 나오는 기능**만 키웠다. LLM이 붙으면 판단이
정교해지지만, 없어도 기능 자체가 사라지지 않는 구조로 만든 것이 이번 사이클의 기준이다.

**왜 이 방향인가**
지금까지 이 시스템은 "이 통화는 해지 문의이고 감정은 강성"까지 답했다. 콜센터의 다음
질문은 "그래서 누가 오늘 무엇을 하느냐"인데 거기서 끊겼다. 분류는 LLM이 필요하지만
**우선순위와 담당 배정은 규칙으로 계산된다** — 그래서 키가 없어도 이쪽은 살릴 수 있었다.

- **이탈 위험 지수**(churnRisk): 해지 의사·타사 비교·위약금 확인·반복 장애·법적 언급·
  보상 요구·신뢰 상실을 가중치로 세고 해결·유지 의사는 감점. 근거 발화를 항상 함께 낸다.
  구현 직후 실제 출력을 돌려보니 셋이 틀렸다 — 근거 인용이 줄바꿈을 넘어 다른 화자의
  말까지 섞였고, 상담사의 "말씀 감사합니다"가 고객 만족으로 잡혔고, 티켓 제목이
  `[해지] 해지`였다. 고객 발화만 훑고, 인용을 줄 안에서 자르고, 유형과 같은 키워드는
  제목에서 빼는 것으로 고쳤다.
- **에스컬레이션 티켓 초안**(ticketDraft): 부서·우선순위·SLA를 붙여 인수인계 문서로 조립.
  없는 값은 줄 자체를 넣지 않는다 — 빈 항목으로 채우면 읽는 사람이 속는다.
- **대화 지표**(callMetrics): 발화 비율·연속 발화·확인 질문·공감·말 끊기. 화자 라벨이
  없으면 null을 준다. 계산할 수 없는 것을 0으로 보여주면 거짓말이 된다.
- **통화 중 스크립트 가이드**(scriptGuide + /api/cc/guide + /guide 화면): 사후 평가를
  사전 예방으로 옮겼다. **LLM을 호출하지 않는 유일한 엔드포인트** — 통화 중에 쓰려면
  즉시 답해야 하고, 규칙으로 답할 수 있는 질문에 LLM은 낭비다. 프로덕션 실측 91ms.
- **VOC 이상 감지**(vocAnomaly·vocThemes): 카테고리를 넘어 **원인 단위**로 센다.
  "불만 3건"에는 넘길 부서가 없지만 "속도 저하 3건 → 네트워크 품질팀"에는 있다.
  표본이 작을 때 비율만 보면 매일 경보가 뜨므로 최소 건수 조건을 함께 건다.
- **QA 코칭 이력**(qaHistory): 통화 한 건의 점수가 아니라 반복 패턴을 본다.
  4건 미만이면 추세를 말하지 않는다 — 2건으로 "하락 추세"는 분석이 아니라 노이즈다.
- **부서 레지스트리**(teams.js): 대시보드와 티켓이 같은 통화를 서로 다른 부서로
  부르던 것을 한 표로 합쳤다. 두 화면이 같은 질문에 다른 답을 하면 둘 다 신뢰를 잃는다.
- **개인정보 마스킹**(piiMask): 콜센터 통화에는 본인확인이 반드시 들어가 주민번호·카드·
  연락처가 전사에 남는다. "개인정보 미수집"을 원칙으로 내세우면서 정작 가장 개인정보가
  많은 경로를 그냥 흘려보내고 있었다. 자릿수를 보존해 가리므로 규칙 검사 결과는 그대로다.

**연구가 찾아낸 가장 아픈 것 — "만들어 놓고 잇지 않았다"**
다섯 관점(실무자·심사관·엔지니어·제품·시장) 조사에서 공통으로 나온 지적이다.
qaHistory(155줄)와 scriptGuide(147줄)는 테스트까지 갖췄는데 **어느 페이지도 import하지
않았고**, 서버가 이미 계산해 내려주는 이탈위험·대화지표·티켓을 /analyze는 하나도 그리지
않았다. 시연용 페이지(/pipeline)가 실무용 페이지보다 많은 걸 보여주는 상태였다.
기능을 더 만들 게 아니라 있는 것을 잇는 것이 이번 사이클 후반의 작업이었다.

**검증에서 나온 major 10건 수정**
6영역 구현 뒤 각 영역을 읽기 전용으로 적대적 검증한 결과다. 특히 두 가지는 "고쳤다"는
보고가 사실과 달랐다.
- 절단 재시도가 이중 과금을 막기는커녕 키웠다. 문턱이 8초인데 상한은 24000토큰이라
  가드가 무효였고, 재시도는 걸리기만 하고 중단돼 이미 돈을 낸 1차 응답까지 버렸다.
- 계약 검증 옵션(stringArrays)은 추가됐지만 **호출부가 하나도 쓰지 않아** 사문 코드였다.
  주석만 완료형이라 다음 독자가 닫힌 결함으로 읽을 상태였다.
- RAG 거절 표현 인식이 6개 패턴 나열이라 "포함되어 있지 않습니다" 같은 흔한 문장을
  놓쳤고, 그러면 정답 행동인 거절 답변이 강등돼 **그 자리에 가장 확신에 찬 문장**이
  들어갔다. 환각을 막으려던 게이트가 환각을 만든 것이다.
- QA 인용 검증은 양쪽으로 틀렸다. 여닫이를 짝짓지 않아 짧은 인용("네")을 만나면 진짜
  인용을 삼켰고(정상 코멘트가 허위로 낙인), 2-gram 겹침은 어미만 맞아도 통과해 전사에
  없는 "환불해 드리겠습니다"가 검증을 통과했다. 인용은 축자여야 하므로 "전사에 그대로
  있는가"로 바꿨다.

**검증**: vitest 164 → **480개** · 빌드 경고 0 · 프로덕션 스모크 8종 통과 ·
/api/cc/guide 실측 91ms(과금 0) · CSP·캐시 헤더 라이브 확인.
한계로 남긴 것: 헤드리스 브라우저 검증이 이 환경에서 동작하지 않아(덤프 0바이트)
화면 렌더는 빌드 산출물·청크 내용·라우팅 200으로만 확인했다.

### 2026-07-30 · 사이클 38 (전수 버그 감사와 최적화 — 6방향 병렬 감사)

백엔드·프론트엔드·빌드/설정·테스트 신뢰성·문서 정합·파이프라인 흐름 6방향으로 전수
감사한 뒤, 확인된 것만 고쳤다. 감사 결과 중 "추측"은 코드로 직접 확인해 걸러냈다.

**검증층의 CPU 한계 (가장 위험했던 것)**
원문 보존 게이트가 전체 Levenshtein DP를 돌았다. diarize 입력 상한이 6000자이므로
최악의 경우 3,600만 셀 + 행마다 배열 할당 → 엣지 런타임 CPU 한도를 넘겨 **요청 자체가
죽는다**. 그 시점에 Claude 토큰은 이미 과금됐고, logCall이 게이트 뒤에 있어 텔레메트리도
남지 않았다. 지금까지 드러나지 않은 이유는 LLM이 원문을 그대로 보존하면 문자열이 완전히
같아 `a === b` 조기 반환에 걸렸기 때문이다 — 조금이라도 달라지는 순간 터진다.
→ `levenshteinCapped(a, b, k, cellBudget)` 신설: ① 길이 차이로 초과 조기 확정
② 공통 접두·접미 절단(보존된 정상 케이스는 여기서 거의 0으로 수렴) ③ 밴드 폭 k의
Int32Array DP + 행 최솟값 조기 종료 ④ 셀 예산 초과 시 계산 포기.
포기했을 때 반환하는 null은 "초과"가 아니라 **"검증 불가"**이고, 게이트는 이를
통과시키지 않는다 — 검증할 수 없으면 원문을 지키는 쪽이 안전하다.
프로덕션 재실측: `preserved_cer: 0`, 라벨 정상 적용.

**비용·가용성**
- 레이트리밋 검사 순서를 뒤집었다(IP → 시간당 전역 → 일일 예산). 예전에는 거부될 요청이
  공유 일일 예산을 먼저 차감해서, 한 IP가 300회를 태우면 그날 남은 시간 동안 **모든
  방문자의 모든 기능**이 데모로 강등됐다. 비용 0으로 서비스를 무력화할 수 있었다.
- STT를 LLM 일일 예산에서 분리(`cc:daily:stt` 600). 27분 녹취 한 건이 청크 30개로
  공유 예산 300 중 30을 먹었다. STT는 Whisper만 쓰고 Claude를 쓰지 않는다.
- voc-report 집계 항목에 길이 제한이 없었다 — `byCategory[].name`이 프롬프트에 그대로
  들어가므로 요청 하나로 수 MB짜리 유료 프롬프트를 만들 수 있었다(다른 엔드포인트는
  전부 상한이 있는데 여기만 구멍). 이름 20자·숫자 상한으로 정규화.
- assist가 레이트리밋보다 먼저 RAG 임베딩(질의 + FAQ 8건)을 호출했다. 429를 받는
  요청도 임베딩 비용을 계속 태웠다 — search.js와 같은 순서로 통일.
- 레이트리밋을 조건부 INSERT 한 문장으로 원자화. check-then-insert였기 때문에 동시
  요청이 모두 "여유 있음"을 읽고 상한을 넘길 수 있었고, 그 함수가 **유료 Claude 캡**에도
  쓰였다. 예산 버킷은 DB 오류 시 fail-closed(오픈소스 강등)로 바꿨다 — 남용 방지의
  fail-open과 예산 캡의 fail-open은 위험이 다르다. 단, D1 바인딩 자체가 없는 환경은
  운영자가 정한 상태이므로 통과시킨다.
- 사다리 아래층 실패가 위층 오류를 덮어써 "무엇이 먼저 무너졌는지"가 사라지던 것 수정
  (claudeErr 우선, oss 오류는 cause).

**프라이버시 규칙 위반 — 문서와 코드의 모순**
README·About·주석이 모두 "IP 미저장"이라 적어놓고, 레이트리밋 버킷 키(`cc:analyze:1.2.3.4`)가
D1 `rate_limit_hits`에 그대로 INSERT되고 있었다. 텔레메트리(ai_calls)만 보면 참이지만
같은 DB 옆 테이블에 (IP, 접속시각)이 평문으로 남았다.
→ `clientKey()`: 날짜를 섞은 솔트로 SHA-256 해시 앞 16자. 하루가 지나면 같은 IP도
다른 키가 되어 장기 추적이 불가능하다. 문구도 실제 동작에 맞게 정정.
→ `tests/privacy.test.js`: 버킷 키에 IPv4/IPv6 원문이 없음, 텔레메트리 컬럼
화이트리스트, 바인딩 값에 원문·IP 형태가 없음을 단정. 규칙을 테스트로 고정했다.

**데이터 소실·이중 과금 (프론트)**
- 화자 분리가 6000자로 잘린 전사를 프론트 상태에 덮어써 뒷부분이 **영구 소실**됐다
  (27분 분할 전사는 한국어 8,000~10,000자). 서버가 `truncated`·`processed_chars`를
  알리고 프론트가 남은 원문을 이어 붙인다. 안내도 표시.
- 분할 전사 루프가 지역 배열을 쓰다가 한 청크 실패에 완료분 전부를 버렸다. 30분을
  기다린 사용자가 빈 화면을 받았다 — 부분 결과를 살려 렌더 + 중단 지점 안내.
- STT IP 한도(30/시)가 MAX_CHUNKS(30)와 정확히 같아, 샘플 데모를 한 번만 써도
  긴 전사가 자기 자신 때문에 429를 맞았다 — 40/시로 여유 확보.
- 샘플 데모 버튼이 wav를 받는 동안 `loading`이 false여서 두 번 클릭 시 비교 전사가
  2개 병렬 실행 → **유료 STT 4건**. `busyRef` 잠금 + 즉시 loading.
- `chunkAudioFile`이 전체 디코드·리샘플·청크 인코딩을 마친 **뒤에** 상한을 검사했다.
  30분 파일이면 수백 MB를 할당한 다음 "지원하지 않는다"고 알렸다 — 디코드 직후 길이
  검사로 앞당기고, 업로드 40MB 상한·형식·0바이트 검사를 서버 호출 전에 추가.
- 녹음 중 페이지를 떠나면 `onstop`이 언마운트 후 발화해 `onDone → run(file)`이
  **STT·화자분리·분석·QA 4개 유료 호출**을 보이지 않는 화면을 위해 실행했다.
  cleanup에서 onstop을 먼저 끊고, MediaRecorder 생성 실패 시 마이크 스트림도 정리
  (실패해도 탭의 마이크 표시가 켜진 채 남았다). 녹음 5분 자동 정지.
- **Turnstile 함정**: 모듈 단위로 컨테이너 하나를 공유해, 파이프라인처럼 동시 요청이
  겹치면 대부분 토큰 없이 끝났다. 지금은 시크릿이 없어 fail-open이라 조용하지만,
  사용자가 Turnstile을 등록하는 순간 대표 데모가 403으로 막힐 상태였다 — 요청마다
  새 컨테이너.

**정직성 표시 복구**
- QA 데모·폴백 점수가 실측처럼 보였다. `llmEstimated`는 `llm === null`일 때만 서는데
  모든 경로가 데모 객체를 넘겨서, 화면의 "(추정)" 라벨이 **죽은 코드**였다. 규칙 비율로
  역산한 값이 LLM 정성 평가와 똑같은 모습으로 표시됐다 → `respond(null, ...)`로 수정.
- 파이프라인이 `notice`를 렌더하지 않아, 라이브 폴백·예산 소진·게이트 작동이 전부
  "라이브 AI는 API 키 등록 시 활성화"라는 배지로만 보였다(키는 이미 등록됨). 심사자가
  "라이브가 아니구나"로 오해할 지점 — 4단계 전부 notice 표시.
- 화자 라벨이 없으면 `agentLines`가 전문을 반환해 고객 발화("했잖아요", "아 진짜")까지
  상담사 감점으로 집계된다. 점수를 조작하지 않고 `speaker_labeled: false`를 응답에
  실어 화면이 고지하게 했다. 프로덕션 실측 확인.
- RAG는 RRF로 정렬하는데 막대·숫자는 코사인 점수였다(1위가 2위보다 짧은 막대).
  정렬 기준으로 통일하고, 키워드 폴백의 정수 가중치를 "12.000 유사도"로 찍던 것도 정정.
- 파이프라인 리포트가 실패 단계를 침묵으로 건너뛰고 "⑥ VOC 누적 완료"를 무조건 적었다
  — 실패를 명시하고 ⑥을 조건부로.

**문서 정합 (제출물 신뢰성)**
- 테스트 110 → 164, 사이클 30 → 38, 분할 임계 "6MB 초과" → 4MB(코드 실제값),
  엔드포인트 7 → 10종, 화면 5 → 8종, "하루 만에" → "이틀간", 상표명 제거.
- **산술적으로 불가능했던 주장 정정**: "테스트 음성(26자) CER 2.6%"는 26자에서 편집거리
  0.676자를 뜻해 성립할 수 없다. 프로덕션에서 재측정 — 정답 35자(정규화), 편집거리 1자,
  **CER 2.9%**, 2.7초. 틀린 글자가 "한빛텔레콤"→"한비텔레콤"이라 도메인 사전 보정으로
  0%가 되는 것도 그대로 유지.
- ENDPOINT_LABEL에 `analyze-batch` 누락 — 운영 지표 표에 영어 원문이 노출됐다.
  기록되는 엔드포인트 전체가 라벨을 갖는지 검사하는 테스트 추가.

**성능·자원**
- CSP 추가. 실제 사용 출처만 허용(jsDelivr 폰트, challenges.cloudflare.com, blob:).
  헤드리스 Chrome으로 DOM 렌더와 콘솔 위반 0건을 확인한 뒤 배포했다.
- `/assets/*` 1년 immutable 캐시(파일명에 해시가 있어 안전), 이름 고정 자산 1일.
- vendor 청크 분리 — 엔트리 244kB → **12.7kB**. 앱 코드를 고쳐도 프레임워크 231kB의
  브라우저 캐시가 유지된다. (Vite 8/rolldown은 manualChunks를 함수로만 받는다)
- CER 계산에 셀 상한(2M). 긴 전사에서 정답 스크립트를 타이핑하면 매 키 입력마다
  최대 1억 셀 DP가 3회 돌아 탭이 멈췄다. 화자 분리 후에는 라벨을 뺀 원문으로 계산
  (라벨이 섞여 turbo가 base보다 나쁜 것처럼 뒤집히던 문제).
- objectURL 언마운트 해제, 복사 토스트 타이머 정리(4개 페이지), 일괄 분할
  메모이제이션, CSV 다운로드 해제 시점(즉시 revoke로 조용히 실패했다), 차트 격자선
  고정(40건이면 41줄이 겹쳤다), base64 헬퍼 중복 제거, persist updater 순수화.

**검증**: vitest 128 → **164개** 전부 통과 · 빌드 경고 0 · 프로덕션 스모크 7종 통과 ·
CSP·캐시 헤더 라이브 확인 · QA/diarize 실호출로 신규 필드와 게이트 동작 실측.
(oxlint는 Windows Application Control이 서명 없는 `.node` 바이너리를 차단해 실행 불가 —
코드 문제가 아니며 빌드는 전 모듈을 통과)

### 2026-07-28 · 사이클 37 (내실 다지기 2 — CORS 실측·성능·회귀 방지)
- **API CORS 실측**: POST 응답에 Access-Control-Allow-Origin 없음 확인 — 제3자 사이트가
  브라우저로 API 응답을 읽을 수 없다 (정적 자산의 * 는 공개 HTML이라 무해). 안전 판정 기록.
- **폰트 CDN preconnect**: Pretendard(jsdelivr) 연결 선행으로 첫 페인트 소폭 개선.
- **스모크에 보안 헤더 회귀 점검** 추가(4종 누락 감지) — 총 7종 점검, 현 프로덕션 전부 통과.
- robots.txt 정상 확인.

### 2026-07-28 · 사이클 36 (내실 다지기 — 보안·품질 감사와 보완)
- 전수 감사: 콘솔 로그·TODO 잔재 없음, 일괄 분석 샘플 분리 정상 확인.
- **보안 응답 헤더 추가**(public/_headers): nosniff·X-Frame-Options DENY(클릭재킹 방지)·
  Referrer-Policy·Permissions-Policy(마이크는 자기 출처만, 카메라·위치·결제 차단).
- **의존성 취약점 정리**: react-router-dom을 정확 버전 7.18.1로 고정 — 7.x 전 버전이
  권고에 걸리는 상황에서 노출 최소(1건, RSC 서버 모드 전용 — 이 앱은 SPA라 미사용
  기능)를 선택. 8.x는 미출시라 현존 최선. 판단 근거를 기록으로 남김.
- About 구조도 최신화: 엔드포인트 9종·Vectorize 반영.

### 2026-07-28 · 사이클 35 (커스텀 설정 영속화 완결)
- QA 커스텀 체크리스트·STT 도메인 사전도 브라우저(localStorage)에 보관 — 새로고침마다
  사라지던 실사용 한계를 3개 커스텀 입력 전부에서 제거 ("설정 가능한 제품" 완결).
- usePersistentState 공용 훅 + loadPersisted/savePersisted 순수 함수 (깨진 JSON·저장
  불가 환경 안전) + 테스트 3개 (총 128개). 서버 미저장 원칙 그대로.

### 2026-07-28 · 사이클 34 (실사용 한계 돌파 — 내 문서 영속화 + 신뢰성 지표 공개)
- **RAG 내 문서 영속화**: 붙여넣은 규정이 새로고침마다 사라지던 실사용 한계 —
  localStorage로 브라우저에만 보관해 재방문에도 유지 (서버 미저장 원칙 그대로),
  "내 문서 비우기" 버튼 제공.
- **신뢰성 지표 공개**: 운영 지표에 "폴백 발동 N회"(AI 실패 시 우아한 강등 횟수)와
  "보존 게이트 차단 N회"(LLM 변형 차단 횟수) 배지 — 실패까지 숨기지 않고 공개하는
  운영 투명성. 테스트 1개 추가 (총 125개).

### 2026-07-28 · 사이클 33 (검증층 전면 확장 — 요약 근거율 + 리포트 숫자 검증)
- LLM 자유 생성 지점 전수 조사 → 남은 두 곳에 결정적 검증층 적용 완결:
- **분석 3줄 요약 근거율**: 요약 표현이 통화 원문과 겹치는 비율을 grounding으로 측정,
  "원문 근거율 N%" 배지 표시.
- **VOC 리포트 숫자 환각 검증**: 리포트 속 모든 숫자를 추출해 실제 집계 수치·파생
  퍼센트 허용 집합과 대조 — 집계에 없는 숫자를 지어내면 해당 수치를 명시해 경고
  (numbers_verified 필드). extractNumbers·allowedNumbers·verifyReportNumbers 순수 함수 +
  테스트 5개 (총 124개).
- 이로써 자유 생성 5지점(화자 분리·RAG 답변·분석 요약·VOC 리포트·QA 점수) 전부에
  결정적 검증·보정층이 붙었다 — "검증이 신뢰를 만든다" 아키텍처 완성.

### 2026-07-28 · 사이클 32 (메인 시스템 한계 돌파 6 — RAG 근거율 검증)
- **구조적 한계**: RAG 답변이 "근거 문서만 사용하라"는 지시를 지켰는지 검증 없이 신뢰 —
  환각이 인용 표시를 달고 나가도 모르는 구멍.
- **돌파**: groundedness — 답변의 문자 2-gram이 검색 문서에서 발견되는 비율을 결정적으로
  측정. 답변 옆에 "근거율 N%" 배지로 투명 표시, 35% 미만이면 원문 확인 권장 공지.
- 화자 분리 게이트(사이클 31)와 같은 철학의 대칭 완성: LLM의 두 자유생성 지점(분리·답변)
  모두 결정적 검증층이 감싼다. charBigrams·groundedness 순수 함수 + 테스트 4개 (총 119개).

### 2026-07-28 · 사이클 31 (메인 시스템 한계 돌파 5 — 화자 분리 원문 보존 게이트)
- **구조적 한계**: 화자 분리는 "원문 단어를 바꾸지 말라"는 프롬프트 지시에만 의존 —
  LLM이 단어를 바꾸거나 문장을 지어내도 검증 없이 QA로 흘러가는 신뢰성 구멍.
- **돌파**: 결정적 검증 게이트 — 분리 결과에서 화자 라벨을 벗겨 원문과 CER 비교,
  15% 초과 변형이면 분리 결과를 버리고 원문 유지 + 사유 공지 (mode: guarded 텔레메트리).
- 통과 시 preserved_cer(원문 대비 변형률)을 응답에 투명 표시.
- "프롬프트 지시는 약속일 뿐, 검증이 있어야 신뢰"라는 설계 철학의 완결.
  stripSpeakerLabels·preservesOriginal 순수 함수 + 테스트 5개 (총 115개).

### 2026-07-28 · 사이클 30 (제출 정합화 — 문서·스모크 동기화)
- README·기능정리를 사이클 25~29 기준으로 전면 동기화: Assist(신규 기능)·Vectorize·
  장시간 분할 전사·QA 일관성·일괄 분석·Opus 5 가동 상태 반영 (테스트 110개, 30사이클).
- 스모크에 assist·analyze-batch 빈 입력 400 점검 추가 (엔드포인트 3종 순회).
- 일괄 분석 라이브 검증: 호출 1회(토큰 432/244)로 2건 처리 — 요금/중립,
  불만/강성+소송 감지 에스컬레이션 정확.

### 2026-07-28 · 사이클 29 (메인 시스템 한계 돌파 4 — 분석 단건 처리)
- **구조적 한계**: 분석 코어는 통화 1건씩만 처리 — 상담 후처리는 실무에서 묶음으로 흐른다.
- **돌파**: /api/cc/analyze-batch — 여러 통화(최대 5건)를 **LLM 호출 1회**로 함께 구조화.
  레이트리밋 1회 소비로 N건 처리, 결과 개수·순서 검증 후 건별 정규화.
- /analyze 하단 "일괄 분석" 섹션: 빈 줄 구분 입력 → 건별 표(유형·감정·요약·에스컬레이션)
  → VOC 일괄 누적. splitCalls 파서 + 테스트 3개 (총 110개).

### 2026-07-28 · 사이클 28 (메인 시스템 한계 돌파 3 — QA 점수 일관성)
- **구조적 한계**: LLM 정성 평가(60점)는 같은 통화도 실행마다 점수가 흔들린다 —
  평가 도구의 신뢰성 문제.
- **돌파 1 — 행동 앵커 루브릭**: 점수 구간(0~4/5~8/9~12/13~16/17~20)마다 구체 행동
  기준을 명시하고 "근거 발화 인용 후 채점"을 강제 — 기준 모호성이 편차의 주원인.
- **돌파 2 — 일관성 밴드**: 결정적 규칙 신호(멘트 이행률·금지 표현)로 기대 구간(±5)을
  만들어 크게 벗어난 점수를 경계로 보정. 보정 시 "(일관성 보정)" 정직 표시.
- 순수 함수(consistencyBand·applyConsistencyBand) + 테스트 3개 (총 107개).

### 2026-07-28 · 사이클 27 (메인 시스템 한계 돌파 2 — STT 장시간 분할 전사)
- **구조적 한계**: Whisper 단발 호출은 6MB·수 분 분량까지 — 실제 콜센터 녹취(수십 분)는
  처리 불가한 입력 규모 한계.
- **돌파**: 클라이언트 분할 파이프라인 — 브라우저에서 디코드 → 16kHz 모노 리샘플 →
  55초 청크 WAV 인코딩 → 순차 전사 → 이어붙이기. 6MB 초과 파일은 오류가 아니라
  자동 분할 전사로 전환 (최대 30청크 ≈ 27분). 진행 상황을 청크 단위로 표시.
- 서버·모델 변경 없음(음성 여전히 미보관). stt IP 상한을 30회/시로 조정해 완주 보장.
- planChunks·encodeWav 순수 함수 + 테스트 4개 (총 104개).
- Vectorize(사이클 26) 라이브 검증: 1차 콜드스타트는 realtime 폴백이 받고,
  2차부터 vector_backend=vectorize로 사전 인덱스 조회 확인. 랭킹 품질 동일(faq02 1위).

### 2026-07-28 · 사이클 26 (메인 시스템 한계 돌파 1 — RAG를 Vectorize 벡터 DB로)
- **구조적 한계**: 기존 RAG는 매 요청마다 코퍼스 전체(13건)를 실시간 임베딩 — 문서가
  수백 건이면 비용·지연이 문서 수에 비례해 폭발하는 구조.
- **돌파**: Cloudflare Vectorize 인덱스(callcenter-faq, bge-m3 1024차원 코사인) 생성 —
  질의 1건만 임베딩해 사전 색인을 조회. 코퍼스가 수백 건이어도 검색 비용 동일.
- 콜드 스타트 자가 시딩(인덱스 비면 FAQ 8건 1회 업서트), 실패 시 실시간 임베딩 →
  키워드 랭킹의 3단 검색 사다리. 내 문서는 서버 미저장 원칙 유지(요청 내 코사인 대조).
- 응답에 vector_backend(vectorize/realtime) 투명 표시 — 화면에서 확인 가능.

### 2026-07-28 · 사이클 25 (신규 시스템 — 실시간 상담 지원 Agent Assist)
- 공고 재분석에서 미대응 문구 발견: "콜센터 AI 서비스 **신규 기능** 연구·개발" — 기존
  5화면은 명시 기능의 재현이라 "스스로 신규 기능을 제안하는 축"이 비어 있었다.
- /assist + /api/cc/assist: 진행 중 대화를 읽고 ① 마지막 고객 발화로 규정 RAG 검색
  ② 근거 기반 다음 응대 멘트 2안(복사 버튼) ③ 에스컬레이션 주의를 실시간 제안.
  STT·NLP·RAG·LLM 네 기술의 결합 지점 — CTI 녹취 회사가 실제로 팔 만한 차세대 기능.
- 3단 사다리·계약 검증·레이트리밋(8회/시)·텔레메트리·데모 폴백 동일 적용.
  허브 카드·네비·About 매핑표 반영. extractQuery·demoAssist 테스트 4개 (총 100개).

### 2026-07-28 · 사이클 24 (비용 표시 정밀화 — 캐시 절감 반영)
- UsageNote가 cache_read_input_tokens를 반영: 캐시 재사용 입력은 10% 단가로 비용 계산,
  "캐시 재사용 N (-90%)" 표시 — 프롬프트 캐싱 효과를 심사자가 눈으로 확인.

### 2026-07-28 · 사이클 23 (배포 스모크 자동화 — npm run smoke)
- scripts/smoke.mjs: 홈·404 SPA 폴백·헬스체크·운영 지표·정적 자산·빈 입력 400까지
  6종 점검을 한 커맨드로. AI 비용·레이트리밋을 소비하지 않는 결정적 점검만 포함.
- 매 배포 후 손으로 하던 curl 검증을 표준화 — 프로덕션 첫 실행 6/6 통과.
- SMOKE_BASE 환경변수로 로컬(wrangler pages dev)에도 동일 적용 가능.

### 2026-07-28 · 사이클 22 (운영 지표 고도화 — 엔진 분포 공개)
- About 운영 지표에 "Claude Opus 5 경유 N회 / 오픈소스 모델 경유 N회" 배지 추가 —
  유료·오픈소스 호출 분포를 실데이터로 투명하게 공개 (liveEngine 분류 + 테스트 2개, 총 96개).
- README를 사이클 19~21 기준으로 갱신 (진짜 사다리·예산 칸막이·캐싱 실측 -75%).
- 캐싱 실증: 동일 voc-report 요청 입력 토큰 1,211 → 299.

### 2026-07-28 · 사이클 21 (메인 시스템 강화 3 — 비용 구조 방어)
- **Opus 프롬프트 캐싱**: 시스템 프롬프트에 cache_control(ephemeral) 브레이크포인트 —
  tools+system 접두사가 5분 캐시되어 연속 시연의 입력 비용·지연 절감. 미달 시 무시라 무해.
  usage에 cache_read_input_tokens도 수집.
- **Claude 전용 일일 예산 칸막이(150회)**: 사다리 안에 내장 — 소진 시 서비스 중단이 아니라
  오픈소스 LLM으로 자연 강등. 유료 과금 폭주를 구조적으로 차단. 테스트 2개 추가 (총 94개).

### 2026-07-28 · 사이클 20 (메인 시스템 강화 2 — 파이프라인 병렬·내결함성)
- ④분석과 ⑤QA를 Promise.allSettled 병렬 실행 — 같은 전사를 읽는 두 호출의 대기를 겹쳐
  원클릭 시연 체감 시간 단축. 진행 표시도 두 단계 동시 점등.
- 단계별 내결함성: 화자 분리 실패 → 보정 전사로 계속, 분석·QA 한쪽 실패 → 다른 쪽 결과
  유지, 분석 실패 시 VOC 누적만 건너뜀. 실패 단계는 "!" 마크 + 사유 표시.
  STT 실패(입력 자체 불가)만 파이프라인을 멈춘다.

### 2026-07-28 · 사이클 19 (메인 시스템 강화 1 — 진짜 3단 사다리)
- **발견한 구조 결함**: 기존 사다리는 키 유무로 Claude "또는" 오픈소스를 고르는 갈림길이라,
  Claude 일시 장애(429/529) 시 Llama를 건너뛰고 곧장 규칙 데모로 추락했다.
- runLlmLadder(_lib/ladder.js): Claude 실패 → 오픈소스 LLM 재시도 → 그래도 실패해야 데모.
  5개 엔드포인트(analyze·qa·search·diarize·voc-report) 전부 공용 헬퍼로 통일 (중복 제거).
- 텔레메트리 mode는 실제 응답한 엔진 기준으로 기록. 테스트 5개 추가 (총 92개).
- 심사 중 Claude 혼잡이 와도 "예시 결과"가 아니라 라이브 AI가 답한다 — 가용성 직결.

### 2026-07-28 · 사이클 18 (VOC CSV 내보내기 — 대시보드에서 보고서로)
- /voc에 "CSV 내보내기(엑셀 호환)": 내장 10건+직접 분석 건 통화 원장을 날짜·제목·유형·
  감정·에스컬레이션·출처 컬럼으로 다운로드. UTF-8 BOM으로 엑셀 한글 호환, 쉼표·따옴표
  이스케이프 CSV 규격 준수. 전 과정 브라우저 내 처리(서버 미전송 원칙 유지).
- buildVocCsv 순수 함수 + 테스트 3개 (총 87개). VOC 집계가 실무 보고 문서로 흘러가는
  마지막 연결 고리.

### 2026-07-28 · 사이클 17 (파이프라인 종합 리포트 — 인수인계 한 장)
- /pipeline 완료 화면에 "전체 리포트 복사": STT 전사→보정 내역→화자 분리→분석
  (유형·감정·요약·조치·에스컬레이션)→QA 점수표·코칭까지 6단계 결과를 인수인계 문서
  한 장으로 조립해 클립보드에 복사. 부분 실행에도 안전한 순수 함수 + 테스트 4개 (총 84개).
- "파이프라인의 끝은 화면이 아니라 실무 문서"라는 마무리 서사.

### 2026-07-28 · 사이클 16 (도메인 사전 커스터마이즈 — 튜닝 실험 UI)
- /stt에 오전사→정정 쌍 편집(최대 3개): 내장 사전 7종에 사용자 쌍을 얹어 보정하고
  전/후 CER 개선 폭이 즉시 재계산된다. 리터럴 매칭이라 특수문자 입력도 안전.
- 사이클 14(QA 커스텀 규칙)와 대칭 구조 — "설정 가능한 도메인 튜닝"으로 공고 키워드
  "STT 모델의 도메인 튜닝" 심화. buildCustomLexicon 순수 함수 + 테스트 5개 (총 80개).

### 2026-07-28 · 사이클 15 (하이브리드 검색 — 벡터+키워드 RRF 융합)
- /search 1단계 검색을 벡터 단독 → **하이브리드(RRF, k=60)**로 격상: bge-m3 코사인
  랭킹과 키워드 랭킹을 Reciprocal Rank Fusion으로 융합해 상위 3건 선정. 정확 용어
  일치(키워드 강점)와 의미 유사(벡터 강점)를 동시에 잡는 실전 IR 설계.
- 임베딩 실패 시 키워드 폴백은 유지. fuseRankings 순수 함수 + 테스트 4개 (총 75개).
- 우대사항 "RAG/Embedding/Vector Search 기반 검색 시스템 설계" 심화 대응.

### 2026-07-28 · 사이클 14 (QA 체크리스트 커스터마이즈 — 콜센터별 규칙)
- /qa에 커스텀 필수 멘트 편집(최대 3개, 라벨+감지 키워드): 규칙 40점을 내장 5개+커스텀에
  균등 재배분해 만점 유지. 서버가 입력을 검증(개수·길이 제한)하고 같은 qaRules.js를
  프론트·서버가 공유해 기준 불일치가 원천적으로 없다. 결과 체크리스트에 "커스텀" 배지.
- "콜센터마다 QA 기준이 다르다"는 실무 요구에 대응 — 데모가 아니라 설정 가능한 제품.
- buildCustomMentions·mentionRuleSet 순수 함수 + 테스트 5개 (총 71개).

### 2026-07-28 · 사이클 13 (파이프라인 마이크 모드 — 내 목소리로 6단계)
- /pipeline에 "🎙 내 목소리로 실행" 추가: 그 자리에서 녹음 → 멈추면 자동으로 같은
  6단계(STT→보정→화자분리→분석→QA→VOC)를 통과. 녹음 미리듣기 제공, 서버 미보관.
- useRecorder 훅을 src/lib로 추출해 /stt와 /pipeline이 공유 (중복 제거).
- 심사자가 상담 상황을 직접 연기해 보는 가장 강한 실증 시연 동선.

### 2026-07-28 · 사이클 12 (실측 운영 지표 — 텔레메트리 대시보드)
- /api/cc/stats: D1 ai_calls를 엔드포인트·모드별 GROUP BY 집계해 공개 (개인정보는
  애초에 미저장이므로 집계에도 없음). About에 5번 섹션 "실측 운영 지표" 추가 —
  누적 호출·라이브 비율 배지 + 기능별 호출 수·라이브 호출·평균 지연 표.
- 집계 가공(가중 평균 지연·라이브 판정·정렬)은 statsSummary.js 순수 함수로 분리해
  테스트 7개 추가 (총 66개). "만들 줄 안다"를 넘어 "운영하며 지표를 본다"는 증명 축.

### 2026-07-28 · 사이클 11 (심사 동선 마감 품질)
- 404 페이지(기존 미지정 경로는 빈 화면), STT 오디오 미리듣기(녹음·파일을 듣고 전사와
  대조), 분석 결과 복사(인수인계 형식)·QA 점수표 복사(코칭 공유 형식), 허브에 원클릭
  파이프라인 카드(★) 추가 — 심사자가 처음 오는 동선의 걸림돌 제거.
- README 전면 최신화 + /api/cc/health(바인딩·엔진 상태) + About 실시간 상태 배지는
  사이클 10.5로 반영 완료 (health 실측: workers_ai=true, d1=true, llm=llama-3.3-70b).

### 2026-07-28 · 사이클 10 (RAG 고도화 — 내 문서 실시간 인덱싱)
- /search에 사용자 문서 붙여넣기(빈 줄 구분, 최대 5건×800자): 질문과 함께 실시간
  임베딩되어 내장 규정 8건과 동일 코퍼스에서 랭킹·인용. "내 문서" 배지로 출처 구분.
- 서버 미저장 원칙 유지. 고정 코퍼스 데모 → 실제 RAG 인덱싱 시스템으로 격상
  (우대사항 "RAG/LLM/NLP 기반 검색 시스템 설계" 정면 대응).
- docs/기능정리.md 추가 — 전체 기능 설명서(지원서 첨부용).

### 2026-07-27 · 사이클 9 (핵심 역량 강화 2 — 화자 분리 + VOC AI 리포트)
- **화자 분리(/api/cc/diarize)**: Whisper 전사는 화자 구분이 없어 Auto QA의 "상담사
  발화만 평가" 규칙이 STT 결과에 제대로 못 붙던 실기능 격차를 해소. LLM이 원문 단어를
  보존하며 상담사:/고객: 태깅만 수행(NLP 기반 근사). 파이프라인 6단계로 확장.
- **VOC AI 리포트(/api/cc/voc-report)**: 대시보드 집계 수치만 LLM에 전달(통화 원문
  미전송)해 핵심 발견 2~3개 + 실행 가능한 권고 액션을 생성 — "집계에서 인사이트까지"
  서사 완결.
- 두 기능 모두 3단 사다리·계약 검증·레이트리밋·텔레메트리 동일 적용.

### 2026-07-27 · 사이클 8 (핵심 역량 강화 — 도메인 튜닝 + 원클릭 파이프라인)
- 공고 문구 재검토에서 미대응 키워드 발견: "STT 모델의 **도메인 튜닝**", "콜센터 특화
  **처리 파이프라인 설계**". 두 가지를 정면 대응.
- **STT 도메인 용어 보정(튜닝 1단계)**: 콜센터 용어 사전(한빛텔레콤·위약금·소액결제 등
  7종)으로 전사 후보정. 실측 오전사("한 밑에 내 콤")를 근거로 사전을 설계했고,
  /stt에서 보정 전/후 CER과 개선 폭을 정량 표시. 순수 함수 + 테스트 5개.
- **/pipeline 원클릭 시연**: 내장 음성 → Whisper STT → 도메인 보정 → LLM 분석 →
  Auto QA → VOC 누적이 버튼 하나로 연속 실행. 시연용 가짜 경로 없이 프로덕션 API
  그대로 사용 — "담당업무가 곧 파이프라인"이라는 중심 서사를 한 화면에.
- 허브 CTA·네비에 파이프라인 진입점 추가, About 매핑표에 두 행 추가. 테스트 59개.

### 2026-07-27 · 사이클 7 (버그 수정 — 오픈소스 LLM 응답 파싱)
- 증상: /analyze /qa /search가 오픈소스 LLM 라이브 대신 데모로 강등 ("JSON을 찾을 수 없습니다").
- 원인: llama-3.3-70b-instruct-fp8-fast가 Workers AI 표준 `{response}`가 아닌
  **OpenAI 호환 `{choices[0].message.content}`** 형태로 응답 → 추출 실패.
- 수정: 두 응답 형태 모두 파싱하도록 workersLlm.js 보강 + 회귀 테스트 추가(54개).
- 프로덕션 검증: analyze가 신규 입력("소송" 언급)을 llm_model 표기와 함께 실시간 분석,
  에스컬레이션 정확 판정. search도 vector 모드 + faq05 인용 답변 확인.
- 교훈: 검증 루프가 스스로 레이트리밋(6회/시)을 소진해 1차 검증이 오탐 — 폴링 검증은
  배포 해시 확인 후 단발 요청으로.

### 2026-07-27 · 사이클 6 (모델 승격 — Claude Opus 5)
- 사용자 지시로 Claude 경로를 Opus 4.8 → **claude-opus-5**로 승격 (가격 동일 $5/$25).
- Opus 5 대응: thinking 기본 활성 → 호출부 max_tokens 상향(analyze·qa 6144, search 4096),
  `output_config.effort: 'medium'`로 분류·요약 작업의 비용·지연 조절.
- UI 표기(UsageNote·배지·About·푸터)와 문서 일괄 갱신. 키 등록 시 Opus 5로 라이브 전환.

### 2026-07-27 · 사이클 5 (핵심 승격 — 오픈소스 LLM 폴백 사다리)
- 문제 인식: API 키 등록 전에는 간판 기능 3종(/analyze /qa /search 답변)이 canned 데모.
- 해결: 폴백 사다리에 **오픈소스 LLM(Llama 3.3 70B, Workers AI)** 층 삽입 —
  `Claude Opus 4.8(키 시) → 오픈소스 LLM(즉시 라이브) → 규칙 데모`.
- 효과: 심사자가 아무 통화나 넣어도 **오늘부터 진짜 AI 분석**. 공고 자격요건
  "오픈소스 AI 모델 활용"에 STT(Whisper)·임베딩(bge-m3)·LLM(Llama)까지 전 층 대응.
- JSON 강제 프롬프트 + extractJson 방어 파싱 + ensureContract 계약 검증 동일 적용.
- 결과 화면에 "오픈소스 LLM 라이브" 초록 배지로 정직하게 구분. 테스트 53개(7개 추가).

### 2026-07-27 · 사이클 4 (UI 전면 개편 — 토스 디자인 언어)
- 사용자 요청("공산품 디자인 탈피, 토스 채용 페이지 스타일")으로 App.css를 처음부터 재작성.
- 토스 디자인 문법 적용: 흰 배경, 테두리 없는 회색 필 카드(#f9fafb/#f2f4f6), 라운드
  16~24px, 단일 블루 포인트(#3182f6), 큰 볼드 타이포(-0.03em), 호버 리프트, Pretendard 폰트.
- 구 ax-workbench 스타일 약 2,400줄 → 사용 클래스만 정리한 새 시트로 교체.
- 파비콘·OG 이미지·VOC 차트 램프까지 팔레트 통일. 배포는 Git push 자동 배포로 전환됨.

### 2026-07-27 · 사이클 3 (첫인상·원클릭)
- **원클릭 풀데모**: /stt에 내장 샘플 음성(합성 목소리, 402KB wav) 버튼 추가 — 클릭 한 번에
  2종 모델 전사 + 정답 스크립트 자동 입력 + CER 비교표까지 자동 완성. 심사자 조작 0회.
- **OG 이미지**: 1200x630 네이비+오렌지 미리보기 이미지 — 지원서에 링크를 넣으면
  카톡·메일·노션에서 카드로 보인다. twitter:card도 large_image로 승격.
- **모바일 점검**: 상단 내비는 ax-workbench의 가로 스크롤+페이드 처리 상속 확인.
- **텔레메트리 실증**: D1 ai_calls에 stt live-turbo 평균 1.6초 / live-base 2.4초 등
  실호출 기록 축적 확인 — 운영 지표 파이프라인 동작 검증.

### 2026-07-27 · 사이클 2 (시연성 강화)
- **마이크 직접 녹음**: MediaRecorder로 /stt에서 원클릭 녹음 → 전사. 심사자가 파일 준비
  없이 그 자리에서 시연 가능.
- **Whisper 2종 CER 비교**: turbo vs base를 같은 음성으로 전사해 CER·지연 비교표 표시.
  실측: 같은 음성에서 turbo는 1글자 오차("한비텔레콤"), base는 "한 밑에 내 콤"으로
  다수 오류 — 모델 성능 평가의 필요성이 그대로 드러남. base 모델은 2MB 이하로 제한
  (바이트 배열 변환 비용).
- **VOC 실시간 누적**: /analyze 결과를 localStorage에 저장(최대 50건, 서버 미전송)해
  대시보드에 내장 10건과 합산. "분석이 쌓여 인사이트가 된다"는 제품 흐름 완성.
- 배포·검증 완료 (양 모델 프로덕션 도메인에서 실음성 확인).

### 2026-07-27 · 사이클 1 (1차 배포)
- 기획안 → 스캐폴드 → 구현 → 테스트 → 배포 → 문서까지 당일 완결.
- 포오스 공식 홈페이지(pos21.co.kr) UI 톤(딥 네이비 + 오렌지) 반영.
- 공고 재분석으로 CER 측정기 추가 — "오픈소스 STT 모델 성능 평가" 문구 1:1 대응.
