# 콜센터 AX 워크벤치 — 기획안

**취업 포트폴리오 3호 · ㈜포오스 "인공지능 융합" 파트(신입) 지원용**
**목표: 7/27 당일 1차 배포. 완벽보다 배포 우선, 개선은 사이클로.**

---

## 1. 회사·공고 분석 (잡코리아 GI_Read/49580595, 마감 2026-07-28)

### 1-1. 회사: ㈜포오스 (POOS)
- 1994년 설립(33년차), 서울 구로, 사원 50명 이하 중소기업. 업종 "컴퓨터·하드웨어·장비".
- **CTI 녹취 시스템·인터페이스 기술로 출발** → ATM 스위칭 장비 → IP-PABX·CTI 신기술
  솔루션 보급. 즉 **콜센터 녹취가 본업인 회사**가 그 녹취 자산 위에 AI 서비스를
  확장하려고 "인공지능 융합" 파트를 뽑는 것이다.
- 시사점: 이 회사에서 AI는 논문이 아니라 **녹취 → STT → 분석 → QA로 이어지는 제품
  파이프라인**이다. 포트폴리오도 정확히 그 파이프라인 순서로 구성한다.

### 1-2. 공고 담당업무 (경력 공고 원문 — 신입이 배울 업무의 지도)
> - STT, LLM, NLP, RAG 기술을 활용한 콜센터 AI 서비스 신규 기능 연구·개발
> - AI 모델 Fine-tuning 및 도메인 최적화 연구·개발
> - **Whisper**, NeMo Conformer 등 오픈소스 STT/ASR 모델의 **성능 평가**, 도메인 튜닝 및 서비스 적용
> - 콜센터 도메인에 특화된 AI 모델과 처리 파이프라인 설계 및 서비스 수준 고도화
> - 콜센터 녹취·STT 데이터 기반 분류, 요약, 검색, VOC·의도 분석 및 상담 품질 평가 자동화(Auto QA) 기능 개발

### 1-3. 신입 자격·우대 ↔ 이 포트폴리오
| 공고(신입) 요건 | 이 포트폴리오의 증명 |
|---|---|
| Whisper 등 STT 모델 실습 경험 (우대) | `/stt` — 오픈소스 Whisper(large-v3-turbo) 서버리스 전사 |
| STT 모델 **성능 평가** (경력 담당업무) | `/stt` — 정답 스크립트 대비 **CER(문자 오류율) 측정기** 내장 |
| RAG·Embedding·Vector Search 개념 이해 (우대) | `/search` — bge-m3 임베딩 → 코사인 유사도 → 근거 인용 답변 |
| NLP 프로젝트 경험 (우대) | `/analyze`·`/qa` — 분류·요약·감정·의도 분석, Auto QA |
| 오픈소스 AI 모델/라이브러리 실습 (자격) | Workers AI의 Whisper·bge-m3 (오픈소스 모델 서빙) |
| AI 서비스 개발에 대한 관심 (자격) | 기획→구현→배포→운영(레이트리밋·텔레메트리)까지 전 주기 |

핵심 메시지: **공고의 담당업무 다섯 줄을 다섯 개의 동작하는 화면으로 1:1 구현했다 —
녹취 회사의 제품 파이프라인 순서 그대로.**

## 2. 기능 설계

### 2-1. /stt — 녹취 전사
- 음성 파일 업로드(mp3/wav/m4a/ogg, ≤6MB) → Pages Functions → **Workers AI
  `@cf/openai/whisper-large-v3-turbo`** (오픈소스 Whisper 계열, 한국어 지원)로 전사.
- AI 바인딩(`env.AI`)만으로 동작 — 별도 API 키 불필요, 즉시 라이브.
- **텍스트 직접 입력 모드 병행**: 음성 없이도 전체 파이프라인 시연 가능.
- **STT 성능 평가(CER)**: 정답 스크립트를 붙여넣으면 전사 결과와 문자 단위
  Levenshtein 거리로 CER(문자 오류율)을 계산해 표시 — 공고의 "오픈소스 STT 모델
  성능 평가" 업무에 직접 대응하는 순수 함수(테스트 포함).
- 전사 결과는 "분석으로 보내기" 버튼으로 `/analyze`에 넘긴다 (sessionStorage 경유).
- 업로드 음성은 전사 후 즉시 폐기 — 서버에 저장하지 않는다.

### 2-2. /analyze — 통화 분석 (LLM 구조화 호출)
- 입력: 전사 텍스트(상담사/고객 구분 형식 권장, 자유 텍스트도 허용).
- Claude Opus 4.8 tool 강제 호출로 구조화 응답:
  ① 문의 유형 분류(가입/해지/요금/불만/기타) ② 3줄 요약 ③ 고객 감정(긍정/중립/부정/강성)
  ④ 고객 의도 키워드 ⑤ 후속 조치 제안 ⑥ **에스컬레이션 판단**.
- 에스컬레이션 원칙(ax-workbench /reviews 패턴 상속): 법적 클레임, 강성 민원,
  보상 요구는 AI가 결론 내리지 않고 `escalate=true`로 담당자에게 올린다 —
  **"반복은 대체하고 판단은 남긴다."**
- `ensureContract`로 응답 계약 검증, 위반 시 데모 폴백.

### 2-3. /qa — 상담 품질 평가 (Auto QA)
**이중 구조** (ax-workbench 금칙어 점검기의 "규칙 사전 스캔 + LLM 사후 평가" 이중 방어 재사용):

| 층 | 방식 | 배점 |
|---|---|---|
| 1층 규칙 기반 | 필수 안내 멘트 5종 체크(인사·본인확인·녹취고지·해결안내·마무리) — 정규식, 결정적 | 40점 |
| 1층 규칙 기반 | 금지 표현 스캔(고객 탓·책임 회피·반말·과잉 약속) — 건당 감점 | 감점 |
| 2층 LLM 평가 | 공감(20)·명확성(20)·문제해결(20) 정성 평가 + 코칭 코멘트 | 60점 |

- 점수 계산(`computeQaScore`)은 순수 함수로 분리해 단위 테스트.
- 규칙 층은 결정적이므로 **데모 모드에서도 실제로 동작**한다 — 키 없이도 절반은 진짜.
- 등급: A(90+) / B(80+) / C(70+) / D(70미만).

### 2-4. /voc — VOC 대시보드
- 내장 샘플 통화 10건(가상 시나리오)의 분석 결과를 유형별·감정별·일별로 집계.
- 경량 인라인 SVG 차트(외부 라이브러리 없음): 유형 막대, 감정 분포, 일별 추이.
- `/analyze`에서 분석한 건을 대시보드에 누적하는 것은 개선 사이클로 미룬다(1차는 내장 데이터).

### 2-5. /search — RAG 미니 데모
- 가상 통신사 상담 FAQ 문서 8건 내장 → 질문과 함께 **Workers AI `@cf/baai/bge-m3`**
  임베딩 → 코사인 유사도 상위 3건 → Claude가 **근거 문단을 인용하며** 답변.
- Vectorize 없이 요청 시 임베딩(문서 8건이라 1회 호출로 충분) — 1차 배포 부담 최소화.
- 폴백 사다리: 임베딩 불가 시 키워드 중복 점수 랭킹 → LLM 불가 시 상위 문서 발췌 답변.

## 3. 기술 선택 이유

| 선택 | 이유 |
|---|---|
| Workers AI Whisper | 공고의 "STT" + "오픈소스 활용 능력" 우대사항에 동시 대응. Pages Functions AI 바인딩만으로 서버리스 STT — 키·GPU·비용 부담 없이 라이브 시연 |
| Claude Opus 4.8 tool 강제 | 분류·QA는 화면이 소비하는 구조화 JSON이 필요 — tool_choice 강제 + ensureContract 검증으로 파싱 실패 원천 차단 (ax-workbench 검증 패턴) |
| bge-m3 임베딩 | 한국어 포함 다국어 임베딩, Workers AI 무추가비용. 문서 8건 규모에 Vectorize는 과설계 |
| React 19 + Vite + Pages | ax-workbench와 동일 스택 — 검증된 구조 재사용으로 당일 배포 가능 |
| D1 | 레이트리밋 + 텔레메트리(개인정보 미저장) 기존 마이그레이션 그대로 |

## 4. 데이터 규칙
- 실제 통화 데이터·실존 기업명 사용 금지. 모든 샘플은 **가상 통신사 "한빛텔레콤"**
  시나리오로 자작 (상표·실존 서비스와 무관한 창작 명칭).
- 음성 샘플: 스크립트만 제작해 두고 녹음은 사용자 본인 목소리로 — `docs/녹음스크립트.md`.
- 개인정보 미수집. 업로드 음성은 전사 후 미보관. 텔레메트리에 입력 내용·IP 미저장.

## 5. 안전장치 (ax-workbench 상속)
- **데모 폴백**: 키·바인딩 없이도 전 기능 흐름 시연 (규칙 기반 모사 응답 + demo 배지)
- **레이트리밋**: IP별 시간당 + 전체 시간당 + 일일 예산 캡(전 엔드포인트 공유 버킷)
- **Turnstile**: 무설정 시 fail-open, 시크릿 등록 시 서버 검증
- **타임아웃 40s + 429/5xx 1회 재시도 + 우아한 강등**(실패 시 데모 응답으로 폴백)
- **응답 계약 검증**: ensureContract — 어긴 응답은 화면에 도달하지 못함
- **토큰·비용 실측 표시**: 생성 1회마다 입력/출력 토큰과 추정 비용 노출
- **텔레메트리**: ai_calls 테이블에 엔드포인트/모드/지연/토큰만 기록

## 6. 화면 구성
```
/          허브 — STT→분석→QA→VOC→검색 파이프라인 소개 + 카드
/stt       녹취 전사 (파일 업로드 | 텍스트 입력)
/analyze   통화 분석 (분류·요약·감정·조치·에스컬레이션)
/qa        Auto QA 점수표 (규칙 40 + LLM 60)
/voc       VOC 대시보드 (내장 10건 집계)
/search    RAG 상담 지식 검색
/about     공고 매핑표 + 시스템 구조도 + 제작기 (심사자용)
```

시스템 구조:
```
브라우저(React) → Pages Functions → Workers AI(Whisper·bge-m3)
                        ├→ Claude Opus 4.8 (tool 강제)
                        └→ D1 (레이트리밋·텔레메트리)
```

## 7. 배포 계획
- Cloudflare Pages 프로젝트 `callcenter-ax` (callcenter-ax.pages.dev)
- wrangler.toml에 AI 바인딩 + D1 바인딩 선언 → `wrangler pages deploy`로 원커맨드 배포
- D1 데이터베이스 `callcenter-ax` 신규 생성, 기존 마이그레이션 2종 적용
- CLAUDE_API_KEY는 시크릿 등록 전까지 데모 폴백으로 운영 (STT·임베딩은 즉시 라이브)

## 8. 1차 범위에서 뺀 것 (개선 사이클로)
- 분석 결과의 VOC 대시보드 실시간 누적 (1차는 내장 샘플)
- Vectorize 기반 대규모 문서 인덱싱
- 화자 분리(diarization) — Whisper 기본 전사만
- QA 체크리스트 편집 UI
